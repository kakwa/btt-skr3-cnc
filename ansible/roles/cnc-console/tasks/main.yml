---
- name: Install python3-serial for cnc-console
  apt:
    name: python3-serial
    state: present
    update_cache: yes
    cache_valid_time: 3600

- name: Deploy cnc-console script
  copy:
    src: cnc-console
    dest: "{{ cnc_console_bin }}"
    owner: root
    group: root
    mode: '0755'
